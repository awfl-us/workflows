main:
  params:
  - "input"
  steps:
  - mainTry:
      steps:
      - mainTry_initResult:
          assign:
          - mainTryResult_31f22d1d: {}
      - mainTry_steps:
          try:
            steps:
            - registerExecForSessionTry:
                steps:
                - registerExecForSessionTry_initResult:
                    assign:
                    - registerExecForSessionTryResult_134cfffa: {}
                - registerExecForSessionTry_steps:
                    try:
                      steps:
                      - registerExecForSessionCreate:
                          try:
                            steps:
                            - registerExecForSessionCreate_call:
                                call: "http.post"
                                args:
                                  url: "${((input.env.BASE_URL + \"/\") + \"firebase/create\")}"
                                  body:
                                    collection: "${\"workflowExecsBySession\"}"
                                    id: "${((input.env.sessionId + \":\") + sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\"))}"
                                    contents:
                                      sessionId: "${input.env.sessionId}"
                                      execId: "${sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\")}"
                                      created: "${sys.now()}"
                                    userId: "${input.env.userId}"
                                  auth:
                                    type: "${\"OIDC\"}"
                                    audience: "${input.env.BASE_URL}"
                                  headers:
                                    x-user-id: "${input.env.userId}"
                                    x-project-id: "${input.env.projectId}"
                                result: "registerExecForSessionCreateResult_2c466cc7"
                          retry: "${http.default_retry}"
                      - registerExecForSessionTry_updateResult:
                          assign:
                          - registerExecForSessionTryResult_134cfffa: "${\"ok\"}"
                    except:
                      as: "registerExecForSessionTryError"
                      steps:
                      - registerExecForSessionSwitch:
                          steps:
                          - registerExecForSessionSwitch_initResult:
                              assign:
                              - registerExecForSessionSwitchResult_78c29a22: {}
                          - registerExecForSessionSwitch_steps:
                              switch:
                              - condition: "${((\"code\" in registerExecForSessionTryError) and (default(map.get(registerExecForSessionTryError, \"code\"), 0) == 409))}"
                                steps:
                                - registerExecForSessionUpdate:
                                    try:
                                      steps:
                                      - registerExecForSessionUpdate_call:
                                          call: "http.post"
                                          args:
                                            url: "${((input.env.BASE_URL + \"/\") + \"firebase/update\")}"
                                            body:
                                              collection: "${\"workflowExecsBySession\"}"
                                              id: "${((input.env.sessionId + \":\") + sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\"))}"
                                              contents:
                                                sessionId: "${input.env.sessionId}"
                                                execId: "${sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\")}"
                                                created: "${sys.now()}"
                                              userId: "${input.env.userId}"
                                            auth:
                                              type: "${\"OIDC\"}"
                                              audience: "${input.env.BASE_URL}"
                                            headers:
                                              x-user-id: "${input.env.userId}"
                                              x-project-id: "${input.env.projectId}"
                                          result: "registerExecForSessionUpdateResult_34d4ad88"
                                    retry: "${http.default_retry}"
                                - registerExecForSessionSwitch_updateResult_0:
                                    assign:
                                    - registerExecForSessionSwitchResult_78c29a22: "${\"ok\"}"
                              - condition: "${true}"
                                steps:
                                - registerExecForSessionRethrow:
                                    raise: "${registerExecForSessionTryError}"
                                - registerExecForSessionSwitch_updateResult_1:
                                    assign:
                                    - registerExecForSessionSwitchResult_78c29a22: "${\"fail\"}"
                      - registerExecForSessionTry_updateResultError:
                          assign:
                          - registerExecForSessionTryResult_134cfffa: "${registerExecForSessionSwitchResult_78c29a22}"
            - registerWorkflowExecLink:
                steps:
                - registerWorkflowExecLink_initResult:
                    assign:
                    - registerWorkflowExecLinkResult_23f07d68: {}
                - registerWorkflowExecLink_steps:
                    switch:
                    - condition: "${(len(default(default(map.get(input.env, \"callingWorkflowExec\"), null), \"\")) > 0)}"
                      steps:
                      - registerWorkflowExecLinkTry:
                          steps:
                          - registerWorkflowExecLinkTry_initResult:
                              assign:
                              - registerWorkflowExecLinkTryResult_4e2a0dfd: {}
                          - registerWorkflowExecLinkTry_steps:
                              try:
                                steps:
                                - registerWorkflowExecLinkCreate:
                                    try:
                                      steps:
                                      - registerWorkflowExecLinkCreate_call:
                                          call: "http.post"
                                          args:
                                            url: "${((input.env.BASE_URL + \"/\") + \"firebase/create\")}"
                                            body:
                                              collection: "${\"workflowExecLinks\"}"
                                              id: "${((default(map.get(input.env, \"callingWorkflowExec\"), null) + \":\") + sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\"))}"
                                              contents:
                                                callingExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                                                triggeredExec: "${sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\")}"
                                                sessionId: "${input.env.sessionId}"
                                                created: "${sys.now()}"
                                              userId: "${input.env.userId}"
                                            auth:
                                              type: "${\"OIDC\"}"
                                              audience: "${input.env.BASE_URL}"
                                            headers:
                                              x-user-id: "${input.env.userId}"
                                              x-project-id: "${input.env.projectId}"
                                          result: "registerWorkflowExecLinkCreateResult_524e6132"
                                    retry: "${http.default_retry}"
                                - registerWorkflowExecLinkTry_updateResult:
                                    assign:
                                    - registerWorkflowExecLinkTryResult_4e2a0dfd: "${\"ok\"}"
                              except:
                                as: "registerWorkflowExecLinkTryError"
                                steps:
                                - registerWorkflowExecLinkSwitch:
                                    steps:
                                    - registerWorkflowExecLinkSwitch_initResult:
                                        assign:
                                        - registerWorkflowExecLinkSwitchResult_107cd5a2: {}
                                    - registerWorkflowExecLinkSwitch_steps:
                                        switch:
                                        - condition: "${(default(map.get(registerWorkflowExecLinkTryError, \"code\"), 0) == 409)}"
                                          steps:
                                          - registerWorkflowExecLinkUpdate:
                                              try:
                                                steps:
                                                - registerWorkflowExecLinkUpdate_call:
                                                    call: "http.post"
                                                    args:
                                                      url: "${((input.env.BASE_URL + \"/\") + \"firebase/update\")}"
                                                      body:
                                                        collection: "${\"workflowExecLinks\"}"
                                                        id: "${((default(map.get(input.env, \"callingWorkflowExec\"), null) + \":\") + sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\"))}"
                                                        contents:
                                                          callingExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                                                          triggeredExec: "${sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID\")}"
                                                          sessionId: "${input.env.sessionId}"
                                                          created: "${sys.now()}"
                                                        userId: "${input.env.userId}"
                                                      auth:
                                                        type: "${\"OIDC\"}"
                                                        audience: "${input.env.BASE_URL}"
                                                      headers:
                                                        x-user-id: "${input.env.userId}"
                                                        x-project-id: "${input.env.projectId}"
                                                    result: "registerWorkflowExecLinkUpdateResult_2eebbb90"
                                              retry: "${http.default_retry}"
                                          - registerWorkflowExecLinkSwitch_updateResult_0:
                                              assign:
                                              - registerWorkflowExecLinkSwitchResult_107cd5a2: "${\"ok\"}"
                                        - condition: "${true}"
                                          steps:
                                          - registerWorkflowExecLinkRethrow:
                                              raise: "${registerWorkflowExecLinkTryError}"
                                          - registerWorkflowExecLinkSwitch_updateResult_1:
                                              assign:
                                              - registerWorkflowExecLinkSwitchResult_107cd5a2: "${\"fail\"}"
                                - registerWorkflowExecLinkTry_updateResultError:
                                    assign:
                                    - registerWorkflowExecLinkTryResult_4e2a0dfd: "${registerWorkflowExecLinkSwitchResult_107cd5a2}"
                      - registerWorkflowExecLink_updateResult_0:
                          assign:
                          - registerWorkflowExecLinkResult_23f07d68: "${registerWorkflowExecLinkTryResult_4e2a0dfd}"
                    - condition: "${true}"
                      steps:
                      - registerWorkflowExecLink_updateResult_1:
                          assign:
                          - registerWorkflowExecLinkResult_23f07d68: "${\"skip\"}"
            - createCallback_try:
                steps:
                - createCallback_try_initResult:
                    assign:
                    - createCallback_tryResult_ad3ee67: {}
                - createCallback_try_steps:
                    try:
                      steps:
                      - createCallback:
                          try:
                            steps:
                            - createCallback_call:
                                call: "events.create_callback_endpoint"
                                args:
                                  http_callback_method: "${\"POST\"}"
                                result: "createCallbackResult_1c1251bf"
                          retry: "${http.default_retry}"
                      - saveCallback:
                          try:
                            steps:
                            - saveCallback_call:
                                call: "http.post"
                                args:
                                  url: "${((input.env.BASE_URL + \"/\") + \"callbacks\")}"
                                  body:
                                    callback_url: "${createCallbackResult_1c1251bf.url}"
                                  auth:
                                    type: "${\"OIDC\"}"
                                    audience: "${input.env.BASE_URL}"
                                  headers:
                                    x-user-id: "${input.env.userId}"
                                    x-project-id: "${input.env.projectId}"
                                result: "saveCallbackResult_2ea0586d"
                          retry: "${http.default_retry}"
                      - awaitCallback:
                          steps:
                          - relay_ingest_tool_call:
                              try:
                                steps:
                                - relay_ingest_tool_call_call:
                                    call: "http.post"
                                    args:
                                      url: "${((input.env.BASE_URL + \"/\") + \"events\")}"
                                      body:
                                        sessionId: "${input.env.sessionId}"
                                        projectId: "${input.env.projectId}"
                                        data:
                                          create_time: "${sys.now()}"
                                          callback_id: "${saveCallbackResult_2ea0586d.body.id}"
                                          content: "${null}"
                                          tool_call: "${input.tool_call}"
                                          cost: "${input.cost}"
                                          background: "${default(map.get(input.env, \"background\"), false)}"
                                          workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                                          timeout_seconds: "${default(map.get(input, \"timeoutSeconds\"), null)}"
                                          status: "${\"\"}"
                                          error: "${null}"
                                        background: "${default(map.get(input.env, \"background\"), false)}"
                                        type: "${\"message\"}"
                                        source: "${\"workflows.tools.CliTools\"}"
                                      auth:
                                        type: "${\"OIDC\"}"
                                        audience: "${input.env.BASE_URL}"
                                      headers:
                                        x-user-id: "${input.env.userId}"
                                        x-project-id: "${input.env.projectId}"
                                    result: "relay_ingest_tool_callResult_43c1222b"
                              retry: "${http.default_retry}"
                          - startProducer:
                              try:
                                steps:
                                - startProducer_call:
                                    call: "http.post"
                                    args:
                                      url: "${((input.env.BASE_URL + \"/\") + \"producer/start\")}"
                                      body:
                                        sessionId: "${input.env.sessionId}"
                                        image: "${null}"
                                      auth:
                                        type: "${\"OIDC\"}"
                                        audience: "${input.env.BASE_URL}"
                                      headers:
                                        x-user-id: "${input.env.userId}"
                                        x-project-id: "${input.env.projectId}"
                                    result: "startProducerResult_6df4db61"
                              retry: "${http.default_retry}"
                      - awaitCallback_1:
                          try:
                            steps:
                            - awaitCallback_call:
                                call: "events.await_callback"
                                args:
                                  callback: "${createCallbackResult_1c1251bf}"
                                  timeout: 3600
                                result: "awaitCallbackResult_1a1596d2"
                          retry: "${http.default_retry}"
                      - checkError:
                          steps:
                          - checkError_initResult:
                              assign:
                              - checkErrorResult_dfed6d1: {}
                          - checkError_steps:
                              switch:
                              - condition: "${((\"error\" in awaitCallbackResult_1a1596d2.http_request.body) and (get_type(map.get(awaitCallbackResult_1a1596d2.http_request.body, \"error\")) == \"map\"))}"
                                steps:
                                - raise:
                                    raise: "${map.get(awaitCallbackResult_1a1596d2.http_request.body, \"error\")}"
                                - checkError_updateResult_0:
                                    assign:
                                    - checkErrorResult_dfed6d1: "${null}"
                              - condition: "${true}"
                                steps:
                                - checkError_updateResult_1:
                                    assign:
                                    - checkErrorResult_dfed6d1: "${awaitCallbackResult_1a1596d2.http_request.body}"
                      - createCallback_try_updateResult:
                          assign:
                          - createCallback_tryResult_ad3ee67: "${awaitCallbackResult_1a1596d2.http_request.body}"
                    except:
                      as: "createCallback_tryError"
                      steps:
                      - logError:
                          try:
                            steps:
                            - logError_call:
                                call: "sys.log"
                                args:
                                  text: "${(\"[logError] \" + createCallback_tryError.message)}"
                                result: "logErrorResult_59a77b5f"
                          retry: "${http.default_retry}"
                      - reRaise:
                          raise: "${createCallback_tryError}"
                      - createCallback_try_updateResultError:
                          assign:
                          - createCallback_tryResult_ad3ee67: "${null}"
            - mainTry_updateResult:
                assign:
                - mainTryResult_31f22d1d:
                    encoded: "${json.encode_to_string(createCallback_tryResult_ad3ee67)}"
                    cost: "${0}"
          except:
            as: "mainTryError"
            steps:
            - ReRaise_error:
                raise: "${mainTryError}"
            - mainTry_updateResultError:
                assign:
                - mainTryResult_31f22d1d: "${null}"
  - return:
      return: "${mainTryResult_31f22d1d}"
