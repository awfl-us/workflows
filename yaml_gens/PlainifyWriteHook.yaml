main:
  params:
  - "input"
  steps:
  - readRaw_decode_content:
      steps:
      - readRaw_call_block:
          steps:
          - buildParams:
              steps:
              - buildParams_initResult:
                  assign:
                  - buildParamsResult_1e4de94: {}
              - buildParams_steps:
                  try:
                    steps:
                    - buildParams_updateResult:
                        assign:
                        - buildParamsResult_1e4de94:
                            filepath: "${input.filepath}"
                  except:
                    as: "buildParamsError"
                    steps:
                    - logError:
                        try:
                          steps:
                          - logError_call:
                              call: "sys.log"
                              args:
                                text: "${(\"[logError] \" + buildParamsError.message)}"
                              result: "logErrorResult_1671996f"
                        retry: "${http.default_retry}"
                    - reRaise:
                        raise: "${buildParamsError}"
                    - buildParams_updateResultError:
                        assign:
                        - buildParamsResult_1e4de94: "${null}"
          - readRaw_await_block:
              steps:
              - readRaw_run:
                  try:
                    steps:
                    - readRaw_run_call:
                        call: "googleapis.workflowexecutions.v1.projects.locations.workflows.executions.run"
                        args:
                          workflow_id: "${\"tools-CliTools${WORKFLOW_ENV}\"}"
                          argument:
                            tool_call:
                              id: "${\"readRaw\"}"
                              type: "${\"function\"}"
                              function:
                                name: "${\"READ_FILE\"}"
                                arguments: "${json.encode_to_string(buildParamsResult_1e4de94)}"
                            cost: 0.0
                            timeoutSeconds: "${default(60, null)}"
                            env:
                              userId: "${input.env.userId}"
                              model: "${input.env.model}"
                              callingWorkflowExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                              BASE_URL: "${input.env.BASE_URL}"
                              background: "${default(true, null)}"
                              projectId: "${input.env.projectId}"
                              sessionId: "${\"plaintify_write_hook\"}"
                              workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                          location: "${sys.get_env(\"GOOGLE_CLOUD_LOCATION\")}"
                          project_id: "${sys.get_env(\"GOOGLE_CLOUD_PROJECT_ID\")}"
                          connector_params:
                            skip_polling: false
                        result: "readRaw_runResult_23ed5c15"
                  retry: "${http.default_retry}"
  - plainifyMessages:
      steps:
      - plainifyMessages_initResult:
          assign:
          - plainifyMessagesResult_3ca7a2ac: []
      - plainifyMessages_steps:
          for:
            value: "plainifyMessagesIdx"
            range: "${[0, (2 - 1)]}"
            steps:
            - plainifyMessages_switch:
                steps:
                - plainifyMessages_switch_initResult:
                    assign:
                    - plainifyMessages_switchResult_25605fb5: {}
                - plainifyMessages_switch_steps:
                    switch:
                    - condition: "${(plainifyMessagesIdx == 0)}"
                      steps:
                      - plainifyMessages_switch_updateResult_0:
                          assign:
                          - plainifyMessages_switchResult_25605fb5:
                              role: "${\"system\"}"
                              content: "${(\"Translate the following code completely into precise, plain technical English. Include all types, methods, data flow, control flow, external interactions, and intent. No omissions. Include the full class signature in code, but the implementation and logic should be in plain English.\n\nTarget filepath: \" + input.filepath)}"
                              tool_calls: "${[]}"
                              tool_call_id: "${null}"
                              create_time: "${sys.now()}"
                              docId: "${null}"
                    - condition: "${(plainifyMessagesIdx == 1)}"
                      steps:
                      - plainifyMessages_switch_updateResult_1:
                          assign:
                          - plainifyMessages_switchResult_25605fb5:
                              role: "${\"user\"}"
                              content: "${(\"Code follows:\n\n\" + map.get(json.decode(readRaw_runResult_23ed5c15.encoded), \"content\"))}"
                              tool_calls: "${[]}"
                              tool_call_id: "${null}"
                              create_time: "${sys.now()}"
                              docId: "${null}"
            - plainifyMessages_updateResult:
                assign:
                - plainifyMessagesResult_3ca7a2ac: "${list.concat(plainifyMessagesResult_3ca7a2ac, plainifyMessages_switchResult_25605fb5)}"
  - chatBlock:
      steps:
      - toolChoice:
          steps:
          - toolChoice_initResult:
              assign:
              - toolChoiceResult_6055570a: {}
          - toolChoice_steps:
              switch:
              - condition: "${((null != null) and (len(null) > 0))}"
                steps:
                - toolChoice_updateResult_0:
                    assign:
                    - toolChoiceResult_6055570a: "${null}"
              - condition: "${true}"
                steps:
                - toolChoice_updateResult_1:
                    assign:
                    - toolChoiceResult_6055570a: "${null}"
      - plainify:
          try:
            steps:
            - plainify_call:
                call: "http.post"
                args:
                  url: "${((input.env.BASE_URL + \"/\") + \"llm/chat\")}"
                  body:
                    messages: "${plainifyMessagesResult_3ca7a2ac}"
                    model: "${\"gpt-5\"}"
                    temperature: 0.8
                    max_completion_tokens: "${null}"
                    response_format:
                      type: "text"
                    sessionId: "${\"plaintify_write_hook\"}"
                    workflow_name: "${sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_ID\")}"
                    tools: "${null}"
                    tool_choice: "${toolChoiceResult_6055570a}"
                  auth:
                    type: "${\"OIDC\"}"
                    audience: "${input.env.BASE_URL}"
                  headers:
                    x-user-id: "${input.env.userId}"
                    x-project-id: "${input.env.projectId}"
                result: "plainifyResult_80c12af"
          retry: "${http.default_retry}"
  - mkdirPlain_output:
      steps:
      - mkdirPlain_call_block:
          steps:
          - buildParams_1:
              steps:
              - buildParams_initResult_1:
                  assign:
                  - buildParamsResult_487c4fcd: {}
              - buildParams_steps_1:
                  try:
                    steps:
                    - buildParams_updateResult_1:
                        assign:
                        - buildParamsResult_487c4fcd:
                            command: "${((\"bash -lc 'mkdir -p $(dirname \" + (\"plain/\" + input.filepath)) + \")'\")}"
                  except:
                    as: "buildParamsError"
                    steps:
                    - logError_1:
                        try:
                          steps:
                          - logError_call_1:
                              call: "sys.log"
                              args:
                                text: "${(\"[logError] \" + buildParamsError.message)}"
                              result: "logErrorResult_1671996f"
                        retry: "${http.default_retry}"
                    - reRaise_1:
                        raise: "${buildParamsError}"
                    - buildParams_updateResultError_1:
                        assign:
                        - buildParamsResult_487c4fcd: "${null}"
          - mkdirPlain_await_block:
              steps:
              - mkdirPlain_run:
                  try:
                    steps:
                    - mkdirPlain_run_call:
                        call: "googleapis.workflowexecutions.v1.projects.locations.workflows.executions.run"
                        args:
                          workflow_id: "${\"tools-CliTools${WORKFLOW_ENV}\"}"
                          argument:
                            tool_call:
                              id: "${\"mkdirPlain\"}"
                              type: "${\"function\"}"
                              function:
                                name: "${\"RUN_COMMAND\"}"
                                arguments: "${json.encode_to_string(buildParamsResult_487c4fcd)}"
                            cost: 0.0
                            timeoutSeconds: "${default(60, null)}"
                            env:
                              userId: "${input.env.userId}"
                              model: "${input.env.model}"
                              callingWorkflowExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                              BASE_URL: "${input.env.BASE_URL}"
                              background: "${default(true, null)}"
                              projectId: "${input.env.projectId}"
                              sessionId: "${\"plaintify_write_hook\"}"
                              workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                          location: "${sys.get_env(\"GOOGLE_CLOUD_LOCATION\")}"
                          project_id: "${sys.get_env(\"GOOGLE_CLOUD_PROJECT_ID\")}"
                          connector_params:
                            skip_polling: false
                        result: "mkdirPlain_runResult_34a7314b"
                  retry: "${http.default_retry}"
      - checkError:
          steps:
          - checkError_initResult:
              assign:
              - checkErrorResult_7d0aa2f6: {}
          - checkError_steps:
              switch:
              - condition: "${(map.get(json.decode(mkdirPlain_runResult_34a7314b.encoded), \"exitCode\") == 0)}"
                steps:
                - checkError_updateResult_0:
                    assign:
                    - checkErrorResult_7d0aa2f6: "${map.get(json.decode(mkdirPlain_runResult_34a7314b.encoded), \"output\")}"
              - condition: "${true}"
                steps:
                - checkError_updateResult_1:
                    assign:
                    - checkErrorResult_7d0aa2f6: "${map.get(json.decode(mkdirPlain_runResult_34a7314b.encoded), \"output\")}"
  - writePlain_encoded:
      steps:
      - writePlain_call_block:
          steps:
          - buildParams_2:
              steps:
              - buildParams_initResult_2:
                  assign:
                  - buildParamsResult_7a7085d7: {}
              - buildParams_steps_2:
                  try:
                    steps:
                    - buildParams_updateResult_2:
                        assign:
                        - buildParamsResult_7a7085d7:
                            filepath: "${(\"plain/\" + input.filepath)}"
                            content: "${plainifyResult_80c12af.body.message.content}"
                  except:
                    as: "buildParamsError"
                    steps:
                    - logError_2:
                        try:
                          steps:
                          - logError_call_2:
                              call: "sys.log"
                              args:
                                text: "${(\"[logError] \" + buildParamsError.message)}"
                              result: "logErrorResult_1671996f"
                        retry: "${http.default_retry}"
                    - reRaise_2:
                        raise: "${buildParamsError}"
                    - buildParams_updateResultError_2:
                        assign:
                        - buildParamsResult_7a7085d7: "${null}"
          - writePlain_await_block:
              steps:
              - writePlain_run:
                  try:
                    steps:
                    - writePlain_run_call:
                        call: "googleapis.workflowexecutions.v1.projects.locations.workflows.executions.run"
                        args:
                          workflow_id: "${\"tools-CliTools${WORKFLOW_ENV}\"}"
                          argument:
                            tool_call:
                              id: "${\"writePlain\"}"
                              type: "${\"function\"}"
                              function:
                                name: "${\"UPDATE_FILE\"}"
                                arguments: "${json.encode_to_string(buildParamsResult_7a7085d7)}"
                            cost: 0.0
                            timeoutSeconds: "${default(60, null)}"
                            env:
                              userId: "${input.env.userId}"
                              model: "${input.env.model}"
                              callingWorkflowExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                              BASE_URL: "${input.env.BASE_URL}"
                              background: "${default(true, null)}"
                              projectId: "${input.env.projectId}"
                              sessionId: "${\"plaintify_write_hook\"}"
                              workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                          location: "${sys.get_env(\"GOOGLE_CLOUD_LOCATION\")}"
                          project_id: "${sys.get_env(\"GOOGLE_CLOUD_PROJECT_ID\")}"
                          connector_params:
                            skip_polling: false
                        result: "writePlain_runResult_632d36cc"
                  retry: "${http.default_retry}"
  - return:
      return: "${(\"plain/\" + input.filepath)}"
