main:
  params:
  - "input"
  steps:
  - readPlain_decode_content:
      steps:
      - readPlain_call_block:
          steps:
          - buildParams:
              steps:
              - buildParams_initResult:
                  assign:
                  - buildParamsResult_67eb72c: {}
              - buildParams_steps:
                  try:
                    steps:
                    - buildParams_updateResult:
                        assign:
                        - buildParamsResult_67eb72c:
                            filepath: "${(\"plain/\" + input.filepath)}"
                  except:
                    as: "buildParamsError"
                    steps:
                    - logError:
                        try:
                          steps:
                          - logError_call:
                              call: "sys.log"
                              args:
                                text: "${(\"[logError] \" + buildParamsError.message)}"
                              result: "logErrorResult_1671996f"
                        retry: "${http.default_retry}"
                    - reRaise:
                        raise: "${buildParamsError}"
                    - buildParams_updateResultError:
                        assign:
                        - buildParamsResult_67eb72c: "${null}"
          - readPlain_await_block:
              steps:
              - readPlain_run:
                  try:
                    steps:
                    - readPlain_run_call:
                        call: "googleapis.workflowexecutions.v1.projects.locations.workflows.executions.run"
                        args:
                          workflow_id: "${\"tools-CliTools${WORKFLOW_ENV}\"}"
                          argument:
                            tool_call:
                              id: "${\"readPlain\"}"
                              type: "${\"function\"}"
                              function:
                                name: "${\"READ_FILE\"}"
                                arguments: "${json.encode_to_string(buildParamsResult_67eb72c)}"
                            cost: 0.0
                            timeoutSeconds: "${default(60, null)}"
                            env:
                              userId: "${input.env.userId}"
                              model: "${input.env.model}"
                              callingWorkflowExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                              BASE_URL: "${input.env.BASE_URL}"
                              background: "${default(true, null)}"
                              projectId: "${input.env.projectId}"
                              sessionId: "${uuid.generate()}"
                              workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                          location: "${sys.get_env(\"GOOGLE_CLOUD_LOCATION\")}"
                          project_id: "${sys.get_env(\"GOOGLE_CLOUD_PROJECT_ID\")}"
                          connector_params:
                            skip_polling: false
                        result: "readPlain_runResult_63dccf25"
                  retry: "${http.default_retry}"
  - plainToCodeMessages:
      steps:
      - plainToCodeMessages_initResult:
          assign:
          - plainToCodeMessagesResult_210c9b1e: []
      - plainToCodeMessages_steps:
          for:
            value: "plainToCodeMessagesIdx"
            range: "${[0, (2 - 1)]}"
            steps:
            - plainToCodeMessages_switch:
                steps:
                - plainToCodeMessages_switch_initResult:
                    assign:
                    - plainToCodeMessages_switchResult_545c3120: {}
                - plainToCodeMessages_switch_steps:
                    switch:
                    - condition: "${(plainToCodeMessagesIdx == 0)}"
                      steps:
                      - plainToCodeMessages_switch_updateResult_0:
                          assign:
                          - plainToCodeMessages_switchResult_545c3120:
                              role: "${\"system\"}"
                              content: "${\"\nYou are PlainToCodeHook. Convert a plain technical-English specification into a complete, compilable source file.\nOperating rules:\n - Infer the target language, package, and imports from the file path and context.\n - Preserve behavior, data flow, side effects, error handling, and formatting.\n - Include the correct package declaration when applicable.\n - Return only the full code of the file; no backticks, no commentary.\n      \"}"
                              tool_calls: "${[]}"
                              tool_call_id: "${null}"
                              create_time: "${sys.now()}"
                              docId: "${null}"
                    - condition: "${(plainToCodeMessagesIdx == 1)}"
                      steps:
                      - plainToCodeMessages_switch_updateResult_1:
                          assign:
                          - plainToCodeMessages_switchResult_545c3120:
                              role: "${\"user\"}"
                              content: "${(\"Plain specification follows:\n\n\" + map.get(json.decode(readPlain_runResult_63dccf25.encoded), \"content\"))}"
                              tool_calls: "${[]}"
                              tool_call_id: "${null}"
                              create_time: "${sys.now()}"
                              docId: "${null}"
            - plainToCodeMessages_updateResult:
                assign:
                - plainToCodeMessagesResult_210c9b1e: "${list.concat(plainToCodeMessagesResult_210c9b1e, plainToCodeMessages_switchResult_545c3120)}"
  - chatBlock:
      steps:
      - toolChoice:
          steps:
          - toolChoice_initResult:
              assign:
              - toolChoiceResult_6055570a: {}
          - toolChoice_steps:
              switch:
              - condition: "${((null != null) and (len(null) > 0))}"
                steps:
                - toolChoice_updateResult_0:
                    assign:
                    - toolChoiceResult_6055570a: "${null}"
              - condition: "${true}"
                steps:
                - toolChoice_updateResult_1:
                    assign:
                    - toolChoiceResult_6055570a: "${null}"
      - plain_to_code:
          try:
            steps:
            - plain_to_code_call:
                call: "http.post"
                args:
                  url: "${((input.env.BASE_URL + \"/\") + \"llm/chat\")}"
                  body:
                    messages: "${plainToCodeMessagesResult_210c9b1e}"
                    model: "${\"gpt-4o\"}"
                    temperature: 0.2
                    max_completion_tokens: "${null}"
                    response_format:
                      type: "text"
                    sessionId: "${uuid.generate()}"
                    workflow_name: "${sys.get_env(\"GOOGLE_CLOUD_WORKFLOW_ID\")}"
                    tools: "${null}"
                    tool_choice: "${toolChoiceResult_6055570a}"
                  auth:
                    type: "${\"OIDC\"}"
                    audience: "${input.env.BASE_URL}"
                  headers:
                    x-user-id: "${input.env.userId}"
                    x-project-id: "${input.env.projectId}"
                result: "plain_to_codeResult_7e868d23"
          retry: "${http.default_retry}"
  - mkdirCode_output:
      steps:
      - mkdirCode_call_block:
          steps:
          - buildParams_1:
              steps:
              - buildParams_initResult_1:
                  assign:
                  - buildParamsResult_4231d0ec: {}
              - buildParams_steps_1:
                  try:
                    steps:
                    - buildParams_updateResult_1:
                        assign:
                        - buildParamsResult_4231d0ec:
                            command: "${((\"bash -lc 'mkdir -p $(dirname \" + input.filepath) + \")'\")}"
                  except:
                    as: "buildParamsError"
                    steps:
                    - logError_1:
                        try:
                          steps:
                          - logError_call_1:
                              call: "sys.log"
                              args:
                                text: "${(\"[logError] \" + buildParamsError.message)}"
                              result: "logErrorResult_1671996f"
                        retry: "${http.default_retry}"
                    - reRaise_1:
                        raise: "${buildParamsError}"
                    - buildParams_updateResultError_1:
                        assign:
                        - buildParamsResult_4231d0ec: "${null}"
          - mkdirCode_await_block:
              steps:
              - mkdirCode_run:
                  try:
                    steps:
                    - mkdirCode_run_call:
                        call: "googleapis.workflowexecutions.v1.projects.locations.workflows.executions.run"
                        args:
                          workflow_id: "${\"tools-CliTools${WORKFLOW_ENV}\"}"
                          argument:
                            tool_call:
                              id: "${\"mkdirCode\"}"
                              type: "${\"function\"}"
                              function:
                                name: "${\"RUN_COMMAND\"}"
                                arguments: "${json.encode_to_string(buildParamsResult_4231d0ec)}"
                            cost: 0.0
                            timeoutSeconds: "${default(60, null)}"
                            env:
                              userId: "${input.env.userId}"
                              model: "${input.env.model}"
                              callingWorkflowExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                              BASE_URL: "${input.env.BASE_URL}"
                              background: "${default(true, null)}"
                              projectId: "${input.env.projectId}"
                              sessionId: "${uuid.generate()}"
                              workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                          location: "${sys.get_env(\"GOOGLE_CLOUD_LOCATION\")}"
                          project_id: "${sys.get_env(\"GOOGLE_CLOUD_PROJECT_ID\")}"
                          connector_params:
                            skip_polling: false
                        result: "mkdirCode_runResult_3ddf1892"
                  retry: "${http.default_retry}"
      - checkError:
          steps:
          - checkError_initResult:
              assign:
              - checkErrorResult_2f9a7d71: {}
          - checkError_steps:
              switch:
              - condition: "${(map.get(json.decode(mkdirCode_runResult_3ddf1892.encoded), \"exitCode\") == 0)}"
                steps:
                - checkError_updateResult_0:
                    assign:
                    - checkErrorResult_2f9a7d71: "${map.get(json.decode(mkdirCode_runResult_3ddf1892.encoded), \"output\")}"
              - condition: "${true}"
                steps:
                - checkError_updateResult_1:
                    assign:
                    - checkErrorResult_2f9a7d71: "${map.get(json.decode(mkdirCode_runResult_3ddf1892.encoded), \"output\")}"
  - writeCode_encoded:
      steps:
      - writeCode_call_block:
          steps:
          - buildParams_2:
              steps:
              - buildParams_initResult_2:
                  assign:
                  - buildParamsResult_75786204: {}
              - buildParams_steps_2:
                  try:
                    steps:
                    - buildParams_updateResult_2:
                        assign:
                        - buildParamsResult_75786204:
                            filepath: "${input.filepath}"
                            content: "${plain_to_codeResult_7e868d23.body.message.content}"
                  except:
                    as: "buildParamsError"
                    steps:
                    - logError_2:
                        try:
                          steps:
                          - logError_call_2:
                              call: "sys.log"
                              args:
                                text: "${(\"[logError] \" + buildParamsError.message)}"
                              result: "logErrorResult_1671996f"
                        retry: "${http.default_retry}"
                    - reRaise_2:
                        raise: "${buildParamsError}"
                    - buildParams_updateResultError_2:
                        assign:
                        - buildParamsResult_75786204: "${null}"
          - writeCode_await_block:
              steps:
              - writeCode_run:
                  try:
                    steps:
                    - writeCode_run_call:
                        call: "googleapis.workflowexecutions.v1.projects.locations.workflows.executions.run"
                        args:
                          workflow_id: "${\"tools-CliTools${WORKFLOW_ENV}\"}"
                          argument:
                            tool_call:
                              id: "${\"writeCode\"}"
                              type: "${\"function\"}"
                              function:
                                name: "${\"UPDATE_FILE\"}"
                                arguments: "${json.encode_to_string(buildParamsResult_75786204)}"
                            cost: 0.0
                            timeoutSeconds: "${default(60, null)}"
                            env:
                              userId: "${input.env.userId}"
                              model: "${input.env.model}"
                              callingWorkflowExec: "${default(map.get(input.env, \"callingWorkflowExec\"), null)}"
                              BASE_URL: "${input.env.BASE_URL}"
                              background: "${default(true, null)}"
                              projectId: "${input.env.projectId}"
                              sessionId: "${uuid.generate()}"
                              workdir: "${default(map.get(input.env, \"workdir\"), null)}"
                          location: "${sys.get_env(\"GOOGLE_CLOUD_LOCATION\")}"
                          project_id: "${sys.get_env(\"GOOGLE_CLOUD_PROJECT_ID\")}"
                          connector_params:
                            skip_polling: false
                        result: "writeCode_runResult_72fc620"
                  retry: "${http.default_retry}"
  - return:
      return: "${input.filepath}"
